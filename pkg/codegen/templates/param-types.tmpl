{{range .}}{{$opid := .OperationId}}
{{range .TypeDefinitions}}
{{if .Comment}}{{.Comment}}{{else}}// {{.TypeName}} defines parameters for {{$opid}}.{{end}}
type {{.TypeName}} {{if .IsAlias}}={{end}} {{.Schema.TypeDecl}}
{{end}}
{{end}}
