# yaml-language-server: $schema=../../../../../configuration-schema.json
package: packagea
generate:
  models: true
  embedded-spec: true
# issue-2383: emit the BearerAuth scopes context key so that packages
# generated from specs $ref'ing this one can alias it.
compatibility:
  enable-auth-scopes-on-context: true
  # EnrichedUser and StatusPostPayload (issue #2288) merge a schema from
  # another document with properties of their own. Later
  # schema-merging-behavior versions reject that; this tests v2's output.
  schema-merging-behavior: v2
output-options:
  skip-prune: true
import-mapping:
  ../packageB/spec.yaml: github.com/oapi-codegen/oapi-codegen/v2/internal/test/references/multipackage/packageB
output: externalref.gen.go
