# From issue-1212: a strict server referring to an external multipart response definition.
openapi: 3.0.0
paths:
  /test:
    get:
      operationId: Test
      responses:
        "200":
          $ref: 'pkg2.yaml#/components/responses/test'
