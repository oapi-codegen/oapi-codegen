# Folded from schemas/schemas.yaml. Compile-only.
#   - AnyType1 / AnyType2: schemas with no type -> Go interface{} (any).
#   - CustomStringType: string with a custom format and x-oapi-codegen-extra-tags.
openapi: "3.0.1"
info:
  version: "1.0.0"
  title: schemas/primitives (schemas fold)
  description: |
    Untyped (any) schemas and a custom-format string with extra struct tags.
paths: {}
components:
  schemas:
    AnyType1: { }
    AnyType2:
      description: |
        AnyType2 represents any type.

        This should be an interface{}
    CustomStringType:
      type: string
      format: custom
      x-oapi-codegen-extra-tags:
        foo: bar
