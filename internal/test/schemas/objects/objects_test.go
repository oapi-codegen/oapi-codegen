package schemasobjects

// issue #1277: additionalProperties:true inside an array item's inline schema;
// also exercises strict-server generation for such responses.
// This is a compile-only test: the source had no assertions.
