# yaml-language-server: $schema=../../../../configuration-schema.json
package: schemasobjects
output: objects.gen.go
# issue #1277: exercises models, client, chi-server, and strict-server generation
# for a response whose content schema is an array of objects with additionalProperties:true.
generate:
  models: true
  client: true
  chi-server: true
  strict-server: true
