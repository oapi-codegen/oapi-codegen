openapi: "3.1.0"
info:
  version: "0.0.1"
  title: base
paths:
  # A direct external response $ref (no path item). The referenced response is
  # renamed via x-go-name in spec-ext.yaml, so the generated client wrapper must
  # reference externalRef0.OutcomeResult (issue #2422).
  /outcome:
    get:
      responses:
        '200':
          $ref: 'spec-ext.yaml#/components/responses/Outcome'
