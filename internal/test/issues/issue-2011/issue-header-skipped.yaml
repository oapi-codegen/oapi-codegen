openapi: 3.0.2
info:
  version: "0.0.1"
paths:
  /foo:
    get:
      responses:
        200:
          description: "endpoint"
          headers:
            "bar":
              schema:
                type: string
          content:
            application/json:
              schema: 
                $ref: "#/components/schemas/data"
components:
  schemas:
    data:
      type: object
      properties:
        foo:
          type: string
