package optionsyamltags

// outputoptions/yaml-tags: compile-only — the source had no test assertions.
// The generated CookieParamsParams struct must include yaml:"..." tags.
// Compilation verifies that the yaml-tags output option produces valid Go code.
