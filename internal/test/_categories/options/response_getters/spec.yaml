# ==========================================================================
# Category: options/response_getters
# Tests:    skip-response-body-getters on/off -> presence/absence of response getter methods.
#
# Folds in (provenance — to migrate from current tree):
#   - outputoptions/response-body-getters/{enabled,skipped}
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: options/response_getters
  version: "1.0.0"
  description: |
    skip-response-body-getters on/off -> presence/absence of response getter methods.
paths: {}
components:
  schemas: {}
