# ==========================================================================
# Category: options/yaml_tags
# Tests:    yaml-tags: emit yaml struct tags alongside json.
#
# Folds in (provenance — to migrate from current tree):
#   - outputoptions/yaml-tags
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: options/yaml_tags
  version: "1.0.0"
  description: |
    yaml-tags: emit yaml struct tags alongside json.
paths: {}
components:
  schemas: {}
