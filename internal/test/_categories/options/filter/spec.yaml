# ==========================================================================
# Category: options/filter
# Tests:    Include/exclude operations by tag and by operationId (--include-tags / --include-operation-ids).
#
# Folds in (provenance — to migrate from current tree):
#   - filter/{operations,tags}
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: options/filter
  version: "1.0.0"
  description: |
    Include/exclude operations by tag and by operationId (--include-tags / --include-operation-ids).
paths: {}
components:
  schemas: {}
