# ==========================================================================
# Category: extensions/x_deprecated_reason
# Tests:    x-deprecated-reason custom deprecation annotation.
#
# Folds in (provenance — to migrate from current tree):
#   - deprecation/ (extension side)
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: extensions/x_deprecated_reason
  version: "1.0.0"
  description: |
    x-deprecated-reason custom deprecation annotation.
paths: {}
components:
  schemas: {}
