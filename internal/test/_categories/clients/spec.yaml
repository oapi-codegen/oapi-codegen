# ==========================================================================
# Category: clients
# Tests:    Client generation: request/response content-type typing (json, octet-stream, multipart, form, text/plain).
#
# Folds in (provenance — to migrate from current tree):
#   - client/
#   - issues/issue-1914 (text/plain coercion)
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: clients
  version: "1.0.0"
  description: |
    Client generation: request/response content-type typing (json, octet-stream, multipart, form, text/plain).
paths: {}
components:
  schemas: {}
