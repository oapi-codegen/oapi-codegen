# ==========================================================================
# Category: parameters/precedence
# Tests:    Operation-level parameters override path-level same-name parameters.
#
# Folds in (provenance — to migrate from current tree):
#   - issues/issue-1180
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: parameters/precedence
  version: "1.0.0"
  description: |
    Operation-level parameters override path-level same-name parameters.
paths: {}
components:
  schemas: {}
