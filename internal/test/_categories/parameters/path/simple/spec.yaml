# ==========================================================================
# Category: parameters/path/simple
# Tests:    Path params, simple style: primitives, arrays (explode/no), objects (explode/no).
#
# Folds in (provenance — to migrate from current tree):
#   - parameters.yaml (path-simple ops)
#   - refactor-branch parameters/path/simple
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: parameters/path/simple
  version: "1.0.0"
  description: |
    Path params, simple style: primitives, arrays (explode/no), objects (explode/no).
paths: {}
components:
  schemas: {}
