# ==========================================================================
# Category: parameters/header/simple
# Tests:    Header params, simple style: primitives/arrays/objects.
#
# Folds in (provenance — to migrate from current tree):
#   - parameters.yaml
#   - refactor-branch parameters/header/simple
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: parameters/header/simple
  version: "1.0.0"
  description: |
    Header params, simple style: primitives/arrays/objects.
paths: {}
components:
  schemas: {}
