# ==========================================================================
# Category: parameters/query/deep_object
# Tests:    Query params, deepObject style (`color[R]=..`): required & optional.
#
# Folds in (provenance — to migrate from current tree):
#   - parameters.yaml
#   - refactor-branch parameters/query/deep_object
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: parameters/query/deep_object
  version: "1.0.0"
  description: |
    Query params, deepObject style (`color[R]=..`): required & optional.
paths: {}
components:
  schemas: {}
