# ==========================================================================
# Category: parameters/content
# Tests:    Content-based params: JSON/text content in path/query/header/cookie; complex object serialization via content type.
#
# Folds in (provenance — to migrate from current tree):
#   - parameters.yaml (content params)
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: parameters/content
  version: "1.0.0"
  description: |
    Content-based params: JSON/text content in path/query/header/cookie; complex object serialization via content type.
paths: {}
components:
  schemas: {}
