# ==========================================================================
# Category: openapi31/content_keywords
# Tests:    3.1 idiom: contentMediaType / contentEncoding on a string -> openapi_types.File / []byte; explicit `format` wins.
#
# Folds in (provenance — to migrate from current tree):
#   - openapi31_content_keywords/
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.1.0
info:
  title: openapi31/content_keywords
  version: "1.0.0"
  description: |
    3.1 idiom: contentMediaType / contentEncoding on a string -> openapi_types.File / []byte; explicit `format` wins.
paths: {}
components:
  schemas: {}
