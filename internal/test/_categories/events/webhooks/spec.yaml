# ==========================================================================
# Category: events/webhooks
# Tests:    OpenAPI 3.1 webhooks: receiver/initiator codegen across frameworks; payload roundtrip; RequestEditor composition.
#
# Folds in (provenance — to migrate from current tree):
#   - webhooks/{chi,echo,fiber,gin,gorilla,iris,stdhttp}
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.1.0
info:
  title: events/webhooks
  version: "1.0.0"
  description: |
    OpenAPI 3.1 webhooks: receiver/initiator codegen across frameworks; payload roundtrip; RequestEditor composition.
paths: {}
components:
  schemas: {}
