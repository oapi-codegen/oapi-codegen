# ==========================================================================
# Category: references/multipackage
# Tests:    Multi-package generation via import-mapping; cross-package types & responses; import-mapping to JSON deps. Defines the shared-model pattern other categories import.
#
# Folds in (provenance — to migrate from current tree):
#   - externalref/{packageA,packageB,petstore}
#   - issues/issue-1087,issue-1093,issue-1212,issue-2010,issue-2113
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: references/multipackage
  version: "1.0.0"
  description: |
    Multi-package generation via import-mapping; cross-package types & responses; import-mapping to JSON deps. Defines the shared-model pattern other categories import.
paths: {}
components:
  schemas: {}
