# ==========================================================================
# Category: bodies/requests
# Tests:    Request bodies: optional vs required, JSON/form/multipart, body+params combos.
#
# Folds in (provenance — to migrate from current tree):
#   - schemas.yaml Issue9
#   - client.yaml (request bodies)
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: bodies/requests
  version: "1.0.0"
  description: |
    Request bodies: optional vs required, JSON/form/multipart, body+params combos.
paths: {}
components:
  schemas: {}
