# ==========================================================================
# Category: bodies/content_types
# Tests:    Multiple/custom content types: json, +json variants (foo/bar/test/problem/vnd.api), text/plain, xml, multipart, octet-stream; correct field routing & type coercion.
#
# Folds in (provenance — to migrate from current tree):
#   - issues/issue-1127,issue-1168,issue-1208-1209,issue-1298,issue-1914,issue-2190
#   - issue-vnd-json/
#   - schemas.yaml Issue127/Issue1051
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: bodies/content_types
  version: "1.0.0"
  description: |
    Multiple/custom content types: json, +json variants (foo/bar/test/problem/vnd.api), text/plain, xml, multipart, octet-stream; correct field routing & type coercion.
paths: {}
components:
  schemas: {}
