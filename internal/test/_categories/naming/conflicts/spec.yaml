# ==========================================================================
# Category: naming/conflicts
# Tests:    Cross-section & operation/schema name collisions; x-go-name preserved through suffixing; avoid conflicting import/package names in comments.
#
# Folds in (provenance — to migrate from current tree):
#   - name_conflict_resolution/
#   - issues/issue-grab_import_names
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: naming/conflicts
  version: "1.0.0"
  description: |
    Cross-section & operation/schema name collisions; x-go-name preserved through suffixing; avoid conflicting import/package names in comments.
paths: {}
components:
  schemas: {}
