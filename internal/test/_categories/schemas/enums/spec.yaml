# ==========================================================================
# Category: schemas/enums
# Tests:    String/integer enums, illegal enum values -> legal Go const names, enum value conflicts / prefixing.
#
# Folds in (provenance — to migrate from current tree):
#   - components.yaml Enum1-5/FunnyValues
#   - schemas.yaml Issue375 (enum in object in array)
#   - issues/issue-illegal_enum_names
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: schemas/enums
  version: "1.0.0"
  description: |
    String/integer enums, illegal enum values -> legal Go const names, enum value conflicts / prefixing.
paths: {}
components:
  schemas: {}
