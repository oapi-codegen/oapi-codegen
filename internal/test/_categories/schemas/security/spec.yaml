# ==========================================================================
# Category: schemas/security
# Tests:    Undefined / unusual security schemes are tolerated without emitting references to undefined types.
#
# Folds in (provenance — to migrate from current tree):
#   - issues/issue-2367
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: schemas/security
  version: "1.0.0"
  description: |
    Undefined / unusual security schemes are tolerated without emitting references to undefined types.
paths: {}
components:
  schemas: {}
