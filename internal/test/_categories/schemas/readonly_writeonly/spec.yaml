# ==========================================================================
# Category: schemas/readonly_writeonly
# Tests:    readOnly / writeOnly / required-readOnly properties.
#
# Folds in (provenance — to migrate from current tree):
#   - components.yaml ReadOnlyWriteOnly
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: schemas/readonly_writeonly
  version: "1.0.0"
  description: |
    readOnly / writeOnly / required-readOnly properties.
paths: {}
components:
  schemas: {}
