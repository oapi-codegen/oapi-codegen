# ==========================================================================
# Category: schemas/nullable
# Tests:    Nullable combinations: required x optional x nullable, nullable arrays/objects; 3.0 `nullable:true` vs 3.1 `type:[...,null]` equivalence (oapi-codegen/nullable).
#
# Folds in (provenance — to migrate from current tree):
#   - components.yaml (nullable props)
#   - openapi31_nullable/spec_3_0.yaml + spec_3_1.yaml
#   - issues/issue-1039
#   - issues/issue-2185 (required array of nullable items)
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: schemas/nullable
  version: "1.0.0"
  description: |
    Nullable combinations: required x optional x nullable, nullable arrays/objects; 3.0 `nullable:true` vs 3.1 `type:[...,null]` equivalence (oapi-codegen/nullable).
paths: {}
components:
  schemas: {}
