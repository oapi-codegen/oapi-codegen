# ==========================================================================
# Category: schemas/defaults
# Tests:    Default values: primitive/nested-object/array defaults, required vs optional combos, deep nesting. (Coverage gap on main today.)
#
# Folds in (provenance — to migrate from current tree):
#   - (new) port from experimental default_values/
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.0.3
info:
  title: schemas/defaults
  version: "1.0.0"
  description: |
    Default values: primitive/nested-object/array defaults, required vs optional combos, deep nesting. (Coverage gap on main today.)
paths: {}
components:
  schemas: {}
