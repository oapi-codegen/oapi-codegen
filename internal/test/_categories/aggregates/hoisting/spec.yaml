# ==========================================================================
# Category: aggregates/hoisting
# Tests:    Anonymous inner schema hoisting into named types: explicit (generate-types-for-anonymous-schemas) & default, incl. webhook/callback positions.
#
# Folds in (provenance — to migrate from current tree):
#   - anonymous_inner_hoisting/{global,implicit}
#
# SCAFFOLD STUB — spec content not yet migrated.
# Each folded case becomes a named schema/operation below with its own
# `# issue #NNNN: ...` provenance comment.
# ==========================================================================
openapi: 3.1.0
info:
  title: aggregates/hoisting
  version: "1.0.0"
  description: |
    Anonymous inner schema hoisting into named types: explicit (generate-types-for-anonymous-schemas) & default, incl. webhook/callback positions.
paths: {}
components:
  schemas: {}
