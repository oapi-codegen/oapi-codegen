# Code generated by specgen from ../../shapes/oneof_closed_variants.yaml. DO NOT EDIT.
openapi: 3.0.3
info:
  title: oneof_closed_variants
  version: 1.0.0
paths:
  /holder:
    post:
      operationId: holder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Holder'
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Holder'
  /holder-inline:
    post:
      operationId: holderInline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InlineHolder'
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InlineHolder'
  /body/ref:
    post:
      operationId: bodyRef
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Subject'
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subject'
  /body/inline:
    post:
      operationId: bodyInline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind]
              properties:
                kind: {type: string}
                note: {type: string}
              discriminator:
                propertyName: kind
                mapping:
                  cat: '#/components/schemas/Cat'
                  dog: '#/components/schemas/Dog'
                  bird: '#/components/schemas/Bird'
              oneOf:
                - $ref: '#/components/schemas/Cat'
                - $ref: '#/components/schemas/Dog'
                - $ref: '#/components/schemas/Bird'
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [kind]
                properties:
                  kind: {type: string}
                  note: {type: string}
                discriminator:
                  propertyName: kind
                  mapping:
                    cat: '#/components/schemas/Cat'
                    dog: '#/components/schemas/Dog'
                    bird: '#/components/schemas/Bird'
                oneOf:
                  - $ref: '#/components/schemas/Cat'
                  - $ref: '#/components/schemas/Dog'
                  - $ref: '#/components/schemas/Bird'
  /body/component:
    post:
      operationId: bodyComponent
      requestBody:
        $ref: '#/components/requestBodies/SubjectBody'
      responses:
        "200":
          $ref: '#/components/responses/SubjectResponse'
  /body/component-inline:
    post:
      operationId: bodyComponentInline
      requestBody:
        $ref: '#/components/requestBodies/InlineSubjectBody'
      responses:
        "200":
          $ref: '#/components/responses/InlineSubjectResponse'
  /body/default:
    post:
      operationId: bodyDefault
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Subject'
      responses:
        default:
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subject'
  /body/headers:
    post:
      operationId: bodyHeaders
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Subject'
      responses:
        "200":
          description: ok
          headers:
            X-Shape:
              required: true
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subject'
components:
  schemas:
    Subject:
      type: object
      required: [kind]
      properties:
        kind: {type: string}
        note: {type: string}
      discriminator:
        propertyName: kind
        mapping:
          cat: '#/components/schemas/Cat'
          dog: '#/components/schemas/Dog'
          bird: '#/components/schemas/Bird'
      oneOf:
        - $ref: '#/components/schemas/Cat'
        - $ref: '#/components/schemas/Dog'
        - $ref: '#/components/schemas/Bird'
    Holder:
      type: object
      properties:
        one:
          $ref: '#/components/schemas/Subject'
        list:
          type: array
          items:
            $ref: '#/components/schemas/Subject'
        map:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/Subject'
    InlineHolder:
      type: object
      properties:
        one:
          type: object
          required: [kind]
          properties:
            kind: {type: string}
            note: {type: string}
          discriminator:
            propertyName: kind
            mapping:
              cat: '#/components/schemas/Cat'
              dog: '#/components/schemas/Dog'
              bird: '#/components/schemas/Bird'
          oneOf:
            - $ref: '#/components/schemas/Cat'
            - $ref: '#/components/schemas/Dog'
            - $ref: '#/components/schemas/Bird'
        list:
          type: array
          items:
            type: object
            required: [kind]
            properties:
              kind: {type: string}
              note: {type: string}
            discriminator:
              propertyName: kind
              mapping:
                cat: '#/components/schemas/Cat'
                dog: '#/components/schemas/Dog'
                bird: '#/components/schemas/Bird'
            oneOf:
              - $ref: '#/components/schemas/Cat'
              - $ref: '#/components/schemas/Dog'
              - $ref: '#/components/schemas/Bird'
        map:
          type: object
          additionalProperties:
            type: object
            required: [kind]
            properties:
              kind: {type: string}
              note: {type: string}
            discriminator:
              propertyName: kind
              mapping:
                cat: '#/components/schemas/Cat'
                dog: '#/components/schemas/Dog'
                bird: '#/components/schemas/Bird'
            oneOf:
              - $ref: '#/components/schemas/Cat'
              - $ref: '#/components/schemas/Dog'
              - $ref: '#/components/schemas/Bird'
    Cat:
      type: object
      additionalProperties: false
      required: [meow]
      properties:
        meow: {type: string}
        collar:
          type: object
          properties:
            color: {type: string}
    Dog:
      type: object
      additionalProperties: false
      required: [bark]
      properties:
        bark: {type: string}
    Bird:
      type: object
      required: [chirp]
      properties:
        chirp: {type: string}
  requestBodies:
    SubjectBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Subject'
    InlineSubjectBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [kind]
            properties:
              kind: {type: string}
              note: {type: string}
            discriminator:
              propertyName: kind
              mapping:
                cat: '#/components/schemas/Cat'
                dog: '#/components/schemas/Dog'
                bird: '#/components/schemas/Bird'
            oneOf:
              - $ref: '#/components/schemas/Cat'
              - $ref: '#/components/schemas/Dog'
              - $ref: '#/components/schemas/Bird'
  responses:
    SubjectResponse:
      description: ok
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Subject'
    InlineSubjectResponse:
      description: ok
      content:
        application/json:
          schema:
            type: object
            required: [kind]
            properties:
              kind: {type: string}
              note: {type: string}
            discriminator:
              propertyName: kind
              mapping:
                cat: '#/components/schemas/Cat'
                dog: '#/components/schemas/Dog'
                bird: '#/components/schemas/Bird'
            oneOf:
              - $ref: '#/components/schemas/Cat'
              - $ref: '#/components/schemas/Dog'
              - $ref: '#/components/schemas/Bird'
