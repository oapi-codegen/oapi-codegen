versions: [v1, v2]
broken:
  v1: "v1 generates a struct for an allOf over an array schema."
description: >-
  allOf decorating a $ref to an array with uniqueItems. Failed with "merging
  two schemas with different UniqueItems", and allOf over arrays dropped the
  items, giving []any.
subject:
  allOf:
    - $ref: '#/components/schemas/Tags'
    - description: Some tags.
components:
  Tags:
    type: array
    uniqueItems: true
    items: {type: string}
samples:
  - '["a", "b"]'
