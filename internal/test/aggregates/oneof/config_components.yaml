# yaml-language-server: $schema=../../../../configuration-schema.json
package: aggregatesoneof
output: components.gen.go
# components.yaml fold: comprehensive oneOf + anyOf marshaling / discriminator cases.
generate:
  models: true
output-options:
  skip-prune: true
