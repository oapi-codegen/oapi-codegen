// Package extensionsxorder — compile-only: the source (extensions/x-order) had no
// runtime assertions; generation correctness is verified by go vet + the
// field-order visible in x_order.gen.go.
package extensionsxorder
