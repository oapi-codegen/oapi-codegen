components:
  schemas:
    DateInterval:
      type: object
      required:
        - name
      properties:
        end:
          type: string
          format: date
          x-order: 2
        start:
          type: string
          format: date
          x-order: 1
    Port:
      type: integer
    PortInterval:
      type: object
      required:
        - start
        - end
      properties:
        end:
          $ref: '#/components/schemas/Port'
          x-order: 2
        start:
          $ref: '#/components/schemas/Port'
          x-order: 1
