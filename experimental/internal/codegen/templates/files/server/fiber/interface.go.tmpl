{{- /*
  This template generates the ServerInterface for Fiber servers.
  Input: []OperationDescriptor
*/ -}}

// ServerInterface represents all server handlers.
type ServerInterface interface {
{{- range . }}
{{ .SummaryAsComment }}
	// ({{ .Method }} {{ .Path }})
	{{ .GoOperationID }}(c fiber.Ctx{{ range .PathParams }}, {{ .GoVariableName }} {{ .TypeDecl }}{{ end }}{{ if .HasParams }}, params {{ .ParamsTypeName }}{{ end }}) error
{{- end }}
}
