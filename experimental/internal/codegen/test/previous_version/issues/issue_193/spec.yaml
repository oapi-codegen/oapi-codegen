# Issue 193: AllOf with additionalProperties merging
# https://github.com/oapi-codegen/oapi-codegen/issues/193
openapi: 3.0.0
info:
  title: test schema
  version: 1.0.0
paths: {}
components:
  schemas:
    Person:
      allOf:
        # common fields
        - type: object
          additionalProperties: true
          required:
            - metadata
          properties:
            metadata:
              type: string
        # person specific fields
        - type: object
          additionalProperties: true
          properties:
            name:
              type: string
            age:
              type: number
